*********************
The lifecycle manager
*********************

The parts data and the actions required to modify the project state
are coordinated by the lifecycle manager. Under its control are
the specification for each part, the planning of actions required
to run a step for a given set of parts, and the execution of the
planned actions.

.. autoclass:: craft_parts.LifecycleManager
   :members:
